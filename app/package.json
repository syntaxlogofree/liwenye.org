{
  "name": "ye.online",
  "version": "1.0.0",
  "description": "headless wordpress template for ye.online",
  "author": "Mot Studio",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "bulma": "^0.7.5",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^4.19.1",
    "eslint-friendly-formatter": "^4.0.1",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^4.0.0",
    "node-sass": "^4.12.0",
    "nuxt-sass-resources-loader": "^2.0.5",
    "push-dir": "^0.4.1",
    "sass-loader": "^7.1.0",
    "vue-style-loader": "^4.1.2"
  }
}
